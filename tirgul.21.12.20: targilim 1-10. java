package com.company;

import java.util.Random;
import java.util.Scanner;

public class Main {

    public static void main(String[] args) {
	// write your code here
        Random r = new Random();
        Scanner s = new Scanner(System.in);
        //targil 1:
        // int - תא זיכרון המסוגל להכיל מספרים שלמים בטווח מוגבל
        // float - בסוף המספר f  תא זיכרון המסוגל להכיל מספרים עם נקודה עשרונית כשמשתמשים בו יש להוסיף

        //targil 2:
        //2. מחלקה ב-java המאפשרת לנו לקבל קלט מהמשתמש/ מהמקלדת.

        //targil 3:
        //פקודה זו מאפשרת לנו להדפיס את מה שנמצא בתוך הסוגריים לדוגמה: תאי זיכרון, מחרוזות וכו'

        //targil 4:
        int x = 5;
        int y = 20 + 12;
        float z = 7.5f;

        //targil 5:

        int a = 5;
        int b = 7;
        int sum = (a+b);
        System.out.println(sum);
        int multiply = (a * b);
        System.out.println(multiply);

        //targil 6:

        String color = ("blue");

        //targil 7:

        System.out.println("please enter your name");
        String name = s.nextLine();
        System.out.println("Hello " + name + "," + " welcome to Java!");

        //targil 8:

        int year = 2020;
        year += 10;
        year -= 30;
        System.out.println(year);
        //the year is 2000

       //targil 9:


        System.out.println("please enter a number: ");
        int num1 = s.nextInt();
        System.out.println("please enter a number: ");
        int num2 = s.nextInt();
        System.out.println("please enter a number: ");
        int num3 = s.nextInt();
        int on_average = ((num1 + num2 + num3)/ 3);
        System.out.println(on_average);

        //targil 10:

        System.out.println("please enter driving speed: ");
        int speed = s.nextInt();
        if (speed > 120){
            System.out.println("too fast!");
        }
        else if (speed < 80){
            System.out.println("too slow!");
        }
        else {
            System.out.println("good driver");
        }
    }
}
